# ---------------------------------------------------------------
# qwen-svg-finetune — dependencies
# ---------------------------------------------------------------
# Probado en:
#   - Python 3.11 (macOS 14, MPS)     -> sin bitsandbytes (LoRA fp16)
#   - Python 3.11 (Colab Free, CUDA)  -> con bitsandbytes (QLoRA 4-bit)
#
# En Colab: descomenta la línea de bitsandbytes.
# En Mac:   dejala comentada (no hay backend CUDA).
# ---------------------------------------------------------------

# Core deep learning
torch>=2.2.0
transformers>=4.45.0

# Data
datasets>=3.0.0

# Training utilities
accelerate>=0.33.0
peft>=0.12.0
trl>=0.12.0          # TRL v1.0 family; check release notes si cambia

# Tokenizer helpers (Qwen usa sentencepiece)
sentencepiece>=0.2.0
protobuf>=4.25.0

# Hub
huggingface_hub>=0.25.0

# SVG evaluación / renderizado
cairosvg>=2.7.1
pillow>=10.0.0       # compositing de PNGs side-by-side en eval.py

# Notebook (opcional local; Colab ya lo trae)
jupyter>=1.0.0
ipywidgets>=8.1.0
bleach>=5.0          # evita warning "no extra named 'css'" al resolver jupyter→nbconvert

# Solo Colab / Linux con CUDA:
# bitsandbytes>=0.43.0
